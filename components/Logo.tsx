export default function Logo() {
  // Placeholder wordmark. Swap for the studio's real logo by dropping a file
  // at /public/logo.svg and replacing this component's contents with an
  // <Image src="/logo.svg" ... /> element.
  return (
    <span className="text-xl font-bold tracking-tight text-foreground">
      Studio<span className="text-muted font-normal">.</span>
    </span>
  );
}
